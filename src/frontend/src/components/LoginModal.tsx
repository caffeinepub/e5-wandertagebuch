// LoginModal is no longer used — replaced by Internet Identity login flow.
// Kept as a stub to avoid import errors during migration.

interface LoginModalProps {
  open: boolean;
  onClose: () => void;
}

export function LoginModal(_props: LoginModalProps) {
  return null;
}
